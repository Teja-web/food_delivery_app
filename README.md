# TaskList Backend

TaskList is a single-user TODO backend with REST JSON `/tasks` endpoints, Drizzle schema support, and Vitest coverage for the v1 API contract.

This README update is also used to verify the GitHub Actions and Coznt webhook flow.
